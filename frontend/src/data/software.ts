/** Mock data — used by the separate "Total Software Assets" page (SoftwareAssets),
    not the main Software inventory page (which now uses data/softwareInventory.ts). */

/** Reported application count — the list only holds the current page of it. */
export const TOTAL_SOFTWARE_APPS = 1500;
