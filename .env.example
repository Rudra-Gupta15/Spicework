# Copy this file to .env.development (or .env.local) and adjust as needed.
# Unset in production — the backend serves the built frontend itself, so
# relative paths resolve correctly (see src/lib/api.ts).
VITE_API_BASE_URL=http://localhost:8000
